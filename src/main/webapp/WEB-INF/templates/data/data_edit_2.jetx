#set(title="数据类型二级字段编辑")
#set(navbar="dataLevelTwoEdit")
#tag layout_block("bodyContent")
<div class="container">

  <legend>数据类型配置项</legend>

  <form id="form1" class="form-horizontal" role="form" action="${CONTEXT_PATH}/create/config/" method="POST">
    <fieldset>

      <div class="form-group">
        <label for="profile" class="col-md-2 control-label">一级组件标识</label>
        <div class="col-md-3 col-lg-3">
          <input type="text" class="form-control" name="name1" id="name1" placeholder="必填" readonly="true" value="${L1_tag}"/>
        </div>
        <p class="help-block col-md-offset-2">如：Register (英文单词)</p>
      </div>

      <div class="form-group">
        <label for="profile" class="col-md-2 control-label">一级组件名称</label>
        <div class="col-md-3 col-lg-3">
          <input type="text" class="form-control" name="name2" id="name2" placeholder="必填" readonly="true" value="${L1_name}"/>
        </div>
        <p class="help-block col-md-offset-2">如：注册 (中文汉字)</p>
      </div>

      <div class="form-group">
        <label for="profile" class="col-md-2 control-label">二级组件名称</label>
        <div class="col-md-3 col-lg-3">
          <input type="text" class="form-control" name="name3" id="name3" placeholder="必填" value="${obj.level2FieldName}"/>
        </div>
        <p class="help-block col-md-offset-2">如：验证码点击次数 (中文汉字)</p>
      </div>

      <div class="form-group">
        <label for="profile" class="col-md-2 control-label">二级组件描述</label>
        <div class="col-md-3 col-lg-3">
          <input type="text" class="form-control" name="name4" id="name4" placeholder="选填" value="${obj.level2FieldDesc}"/>
        </div>
      </div>

      <!-- 确认按钮 -->
      <div class="form-group well well-sm">
        <div class="col-md-offset-2 col-md-10 col-lg-8">
          <p align="lefts" class="bg-danger hide" id="resultInfo"></p>
          <input type="button" class="btn btn-primary" id="confirm" value="确认"></input>
          <input id="cancel_btn" class="btn" type="button" value="返回"/>
        </div>
      </div>

    </fieldset>
  </form>
</div>
#end
#tag layout_block("scriptContent")
<!-- 隐藏域 -->
<input type="hidden" id="name5" name="name5" value="${L1_id}" />
<script>

    var showResult = function(msg){
        $('#resultInfo').text(msg);
	    $('#resultInfo').removeClass("hide");
    }

    var myValidator = function(){
        var msg = '';
        var result = true;

        var value1 = $("#name1").val();
        var value2 = $("#name2").val();
        var value3 = $("#name3").val();

        if(value1 == '' ||
           value2 == '' ||
           value3 == '' ){
           msg = '必填项不能为空值!'
           result = false;
        }

        if(!result){
           showResult(msg);
        }

        return result;
    }

    $(document).ready(function () {

        $("#confirm").on('click', function () {

            var tag = '${tag}';
            var url = '${CONTEXT_PATH}/dataType/' + tag + 'LevelTwo';
            var data = '';
            var id = '${id}';

            var value1 = $("#name1").val();
            var value2 = $("#name2").val();
            var value3 = $("#name3").val();
            var value4 = $("#name4").val();
            var value5 = $("#name5").val();

            if(tag == 'modify'){
                data = {
                    L1_tag              :value1,
                    L1_name             :value2,
                    L2_name             :value3,
                    L2_desc             :value4,
                    L1_id               :value5,
                    id                  :id
                };
            }else if(tag == 'add'){     // 新增不需要指定主键ID
                data = {
                    L1_tag              :value1,
                    L1_name             :value2,
                    L2_name             :value3,
                    L2_desc             :value4,
                    L1_id               :value5,
                };
            }

            if(myValidator()){
                $.ajax({
                    url                 :url,
                    contentType         :"application/json",
                    data                :data,
                    dataType            :"text",
                    type                :"GET",
                    success:function (data) {
                        var json = JSON.parse(data);
                        if (json.code == 200) {
                            showResult(json.info);
                        } else {
                            showResult("警告：" + json.info);
                        }
                    }
                });
            }

        })

        $("#cancel_btn").on('click', function () {
            window.location.href='${CONTEXT_PATH}/dataType/listLevelTwo?L1_id=${L1_id}&L1_tag=${L1_tag}&L1_name=${L1_name}';
        })

    });

</script>
#end
#include("/include/form-layout.jetx")
